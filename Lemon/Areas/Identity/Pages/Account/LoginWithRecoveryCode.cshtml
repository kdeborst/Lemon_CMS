@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Verficatie Herstelcode";
}

<h1 class="text-warning">@ViewData["Title"]</h1>
<hr />
<p>
    Je hebt een ons gevraagd om in te loggen met een herstelcode. Deze login 
    wordt niet onthouden totdat je een authenticator app code gebruikt of zet 2FA uit en log nogmaals in.
</p>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Input.RecoveryCode"></label>
                <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" />
                <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-warning">Inloggen</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
